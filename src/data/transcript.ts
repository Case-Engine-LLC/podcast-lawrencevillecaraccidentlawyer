/**
 * Episode 1 Transcript
 *
 * Intentionally empty until the Yari Lawson "You Interview" episode is
 * recorded and transcribed. The site auto-hides the Transcript tab when this
 * array is empty (see src/themes/v1/components/EpisodeContent.tsx and
 * src/lib/data.ts).
 *
 * DO NOT seed this with placeholder content — the prior version contained an
 * MVP Accident Attorneys / Brett Sachs transcript that was the wrong attorney
 * for this Lawson Personal Injury podcast site and surfaced live in production.
 */

export const episodeTranscript: Array<{
  timestamp: string
  speaker: string
  text: string
}> = []
